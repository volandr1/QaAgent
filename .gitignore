# .NET build
**/bin/
**/obj/

# Тимчасові артефакти прогонів (звіти/TRX) — не комітимо (per-target теж)
artifacts/**/reports/
artifacts/**/test-results/
artifacts/reports/
artifacts/test-results/

# Зберігаємо: artifacts/<Target>/schema-snapshot.json (baseline для diff/self-healing)
#            generated/ApiTests/Generated/<Target>/*.cs (тести — основа для self-healing)

# Користувацькі таргети UI (приватні URL) — локальні
artifacts/custom-targets.json

# Rider/IDE
.idea/
*.user
